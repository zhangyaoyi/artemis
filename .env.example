# ==============================================================================
# Artemis Environment Configuration Template
# ==============================================================================
# Copy this file to .env and fill in your API credentials:
# cp .env.example .env

# LLM Providers (Configure at least one)
GEMINI_API_KEY=
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPEN_ROUTER_API_KEY=
XAI_API_KEY=

# Google Cloud Vision OCR (Optional - for advanced OCR processing)
OCR_API_KEY=

# Android ADB Connectivity (Optional - defaults to 127.0.0.1:5037)
ADB_HOST=127.0.0.1
ADB_PORT=5037
# Optional exclusive device target. Accepts a normal ADB transport serial or a
# stable hardware serial; bare hardware serials use task-time mDNS reconnection.
# When set, Artemis rejects other devices instead of falling back to them.
# ADB_DEVICE_SERIAL=
# Keep the display awake, falling back to a wake lock or safe heartbeat when needed.
# Set to false if Artemis is used with a personal device whose screen should time out.
ARTEMIS_KEEP_DEVICE_AWAKE=true
# UI hierarchy backend: auto (Accessibility Helper, UIAutomator2 fallback) | helper | uiautomator
ARTEMIS_HIERARCHY_BACKEND=auto
# Let tasks install / upgrade the Accessibility Helper APK on a device they hold.
# Set to false on shared or personal phones: then only `artemis helper install` installs it.
ARTEMIS_HELPER_AUTO_INSTALL=true
# Explicitly allow tasks to interact with Android's secure keyguard.
# Credentials may be included in local prompts and traces when enabled.
ARTEMIS_ALLOW_SECURE_KEYGUARD_AUTOMATION=false
# Numeric PIN (4-16 digits) used to unlock the phone over ADB before each task.
# Only used when ARTEMIS_ALLOW_SECURE_KEYGUARD_AUTOMATION=true. Keep it local.
ARTEMIS_DEVICE_UNLOCK_PIN=

# Artemis Execution Defaults (flash or pro)
ARTEMIS_DEFAULT_PROFILE=flash
